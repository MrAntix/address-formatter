'use strict';

angular.module('formatsModule', [
    'formatsServiceModule',
    'formatsListDirectiveModule'
])
    .controller(
        'formatsController',
        [
            '$scope',
            function($scope) {

            }
        ]);